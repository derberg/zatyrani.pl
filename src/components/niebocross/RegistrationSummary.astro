---
interface Props {
  t: (key: string) => string;
}

const { t } = Astro.props;
---

<div class="rounded-xl border-2 border-emerald-200 bg-emerald-50 p-6">
  <h3 class="text-lg font-bold text-slate-900">{t("niebocross.summary.title")}</h3>
  <div class="mt-4 space-y-2 text-sm">
    <div class="flex justify-between">
      <span class="text-slate-700">{t("niebocross.summary.race_fees")}</span>
      <span class="font-semibold text-slate-900" id="raceFees">0 zł</span>
    </div>
    <div class="flex justify-between">
      <span class="text-slate-700">{t("niebocross.summary.tshirt_fees")}</span>
      <span class="font-semibold text-slate-900" id="tshirtFees">0 zł</span>
    </div>
    <div class="h-px bg-emerald-200"></div>
    <div class="flex justify-between text-base">
      <span class="font-semibold text-slate-900">{t("niebocross.summary.total")}</span>
      <span class="font-bold text-emerald-700" id="totalAmount">0 zł</span>
    </div>
    <div class="mt-3 rounded-lg bg-white/50 p-3">
      <p class="text-xs text-slate-600">
        💚 {t("niebocross.summary.charity_info")} <span class="font-semibold" id="charityAmount">0.00 zł</span>
      </p>
    </div>
  </div>
</div>
