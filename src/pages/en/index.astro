---
import Layout from "../../layouts/Layout.astro";
import IndexContent from "../../components/content/Index.astro";
import { t as tBase } from "../../utils/i18n";

const t = (key: string) => tBase(key, 'en');
---

<Layout title={t("home.meta_title")} t={t}>
  <IndexContent t={t} />
</Layout>
