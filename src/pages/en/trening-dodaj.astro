---
import Layout from "../../layouts/Layout.astro";
import TreningDodajContent from "../../components/content/TreningDodaj.astro";
import { t } from "../../utils/i18n";

const tEn = (key: string) => t(key, "en");
---

<Layout t={tEn} title={tEn("add_training.meta_title")}>
  <TreningDodajContent t={tEn} />
</Layout>
